<Window x:Class="Loopback.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:Loopback.Properties"
        Title="{DynamicResource WindowTitle}" Height="560" Width="630" Closing="Window_Closing" Icon="assets/Windows.png">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center">
            <Button Name="btnGithub" Grid.Row="0" Width="40" Height="30" Click="btnGithub_Click" Margin="10,0" Content="😊"/>
            <Button Name="btnLanguage" Grid.Row="0" Width="80" Height="30" Click="btnLanguage_Click" Margin="10,0" Content="{DynamicResource LanguageButton}"/>
            <Button Name="btnSelectAll" Grid.Row="0" Width="100" Height="30" Click="btnSelectAll_Click" Margin="10,0" Content="{DynamicResource SelectAllButton}"/>
            <Button Name="btnDeselectAll" Grid.Row="0" Width="100" Height="30" Click="btnDeselectAll_Click" Margin="10,0" Content="{DynamicResource DeselectAllButton}"/>
            <Button Name="btnSave" Grid.Row="0" Width="100" Height="30" Click="btnSave_Click" Margin="10,0" Content="{DynamicResource SaveButton}"/>
            <Button Name="btnRefresh" Grid.Row="0" Width="100" Height="30" Click="btnRefresh_Click" Margin="10,0" Content="{DynamicResource RefreshButton}"/>

        </StackPanel>
        <DataGrid x:Name="dgLoopback" AutoGenerateColumns="False" Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="{DynamicResource ExemptColumn}" Width="60" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox Name="dgcbLoop" IsChecked="{Binding LoopUtil, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Click="dgcbLoop_Click" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="{DynamicResource AppNameColumn}" Binding="{Binding Path=displayName}" />
            </DataGrid.Columns>
        </DataGrid>
        <StatusBar x:Name="SBar" Grid.Column="0" Grid.Row="3" VerticalAlignment="Bottom" Background="LightBlue" DockPanel.Dock="Bottom" Height="30">
            <StatusBarItem>
                <TextBlock FontWeight="Bold"><Run Text="{DynamicResource StatusLabel}"/></TextBlock>
            </StatusBarItem>
            <StatusBarItem Grid.Column="1">
                <TextBlock Name="txtStatus"></TextBlock>
            </StatusBarItem>
        </StatusBar>

    </Grid>
</Window>
